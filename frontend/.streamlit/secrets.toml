# Streamlit secrets — loaded automatically by st.secrets
# DO NOT commit this file. Add frontend/.streamlit/secrets.toml to .gitignore
#
# Local dev:    set BACKEND_URL to localhost
# Production:   set BACKEND_URL to your Render service URL
#               e.g. https://lexqa.onrender.com

BACKEND_URL = "http://localhost:8000"
